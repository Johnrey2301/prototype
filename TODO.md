# TODO - JS Login/Signup (AJAX)

- [x] Create `dboard/js/auth.js` to handle login + signup via fetch()
- [x] Update `dboard/login.html` to show `#auth-message` and load `./js/auth.js`
- [x] Update `dboard/signup.html` to show `#auth-message` and load `./js/auth.js`
- [ ] (Optional) Run a quick manual test in a PHP-enabled local server:
  - [ ] Signup with new username -> should create account and redirect
  - [ ] Signup with existing username -> should show error message
  - [ ] Login with valid credentials -> should redirect to admin/partner dashboard
  - [ ] Login with invalid credentials -> should show error message

